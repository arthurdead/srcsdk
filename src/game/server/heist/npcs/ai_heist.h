#ifndef AI_HEIST_H
#define AI_HEIST_H

#pragma once

#include "ai_default.h"
#include "ai_task.h"
#include "ai_condition.h"

enum
{
	LAST_HEIST_SCHEDULE = LAST_SHARED_SCHEDULE,
};

enum 
{
	LAST_HEIST_TASK = LAST_SHARED_TASK,
};

enum 
{

	LAST_HEIST_CONDITION = LAST_SHARED_CONDITION,
};


#endif
